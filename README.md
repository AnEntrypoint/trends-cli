# Google Trends CLI

Atomic command-line tool for Google Trends lookups.

## Usage

```bash
node trends "artificial intelligence"
node trends "python"
```

## Output

JSON: query, success, url, timestamp, data (array of date/value pairs).

## Requirements

```bash
npm install playwright
```
